$kind: http-request
description: "Generated from cURL: curl -X POST \"{{base_url}}/embeddings\" -H \"api-key: {{api-key}}\" -H \"Content-Type: application/json\" -d '{\"model\":\"text-embedding-ada-002\",\"input\":\"The quick brown fox jumps over the lazy dog\"}'"
url: "{{base_url}}/embeddings"
method: POST
headers:
  api-key: "{{api-key}}"
  Content-Type: application/json
body:
  type: json
  content: '{"model":"gpt-4o","input":"The quick brown fox jumps over the lazy dog"}'
scripts:
  - type: afterResponse
    code: |-
      pm.test('Embeddings endpoint not supported (403 expected)', function () {
          pm.expect(pm.response.code).to.be.oneOf([403, 404, 400, 501]);
      });

      pm.test('Response is JSON', function () {
          pm.expect(pm.response.headers.get('Content-Type')).to.include('application/json');
      });
    language: text/javascript
order: 1000
